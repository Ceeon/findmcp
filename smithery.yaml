startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
    additionalProperties: false
  commandFunction: |
    function getCommand(config) {
      return {
        command: "node",
        args: ["src/index.js"],
        env: {}
      };
    }

command:
  function: node src/websocket.js